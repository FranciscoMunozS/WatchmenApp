<!DOCTYPE html>
<html>
  <head>
    <title>Gobierno Regional de Los Ríos</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </head>

  <body class="bg-gradient-primary">
    <% if user_signed_in? %>
      <body id="page-top">
      <div id="wrapper">
    <% end %>

    <% if user_signed_in? %>
      <%= render 'assets/sidebar' %>
    <% end %>
    <div id="content-wrapper" class="d-flex flex-column">
      <div id="content">
        <% if user_signed_in? %>
          <%= render 'assets/navbar' %>
        <% end %>
        <div class="container-fluid">
          <%= render 'assets/alert' %>
          <%= yield %>
          <% if user_signed_in? %>
            <%= render 'assets/fab' %>
          <% end %>
        </div>
      </div>
    </div>
  </body>
</html>
